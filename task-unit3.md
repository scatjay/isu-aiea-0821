# 任務：把我的 Moodle 接起來，讀出我自己的授課紀錄

> 這是義守大學 AIEA 工作坊（2026-08-21）單元 3 的任務檔。
> 把它放進你的工作目錄，然後對 Claude 說：**「讀 任務-單元3.md 照做」**
>
> 不用自己打指令。以下每一步都是講給 Claude 聽的。

---

## 給 Claude 的指示

你正在協助一位大學老師，把他自己的 Moodle 接起來，讀出他過去幾年的授課紀錄。

**這位老師不是工程師。** 請一步一步做，每做完一步用一句話說明發生了什麼，
不要一次丟一大串技術細節。遇到選擇時，直接幫他選最穩的那個，然後告訴他你選了什麼。

### 第一步：把工具拉下來

```
git clone https://github.com/scatjay/isu-moodle-mcp.git
cd isu-moodle-mcp
pip install -r requirements.txt
```

只有兩個相依套件（`requests` 和 `mcp`），很快。

裝完把 `.mcp.json.範例` 複製成 `.mcp.json`，並且：

- `args` 裡的路徑改成這台電腦上 `server.py` 的實際位置
- `MOODLE_URL` 填 `https://moodle.isu.edu.tw`
- `MOODLE_TOKEN` 先留空，下一步才會有

### 第二步：讓老師自己去拿 token

**🔴 這一步你不要代勞，也不要問他的密碼。**

請這樣告訴他：

> 請你自己開一個終端機（命令提示字元），切到 `isu-moodle-mcp` 資料夾，跑這一行：
>
> ```
> python get_token.py https://moodle.isu.edu.tw
> ```
>
> 它會問你的 Moodle 帳號密碼。**請在那個終端機裡打，不要打進這個對話框。**
> 對話紀錄會被保存，密碼一旦出現在裡面就等於外洩。
>
> 成功的話它會把 token 寫進 `.env`，只印前幾碼給你看。

然後等他回報結果。

### 第三步：分岔——看第二步成不成功

#### 情況 A：拿到 token 了

把 token 填進 `.mcp.json`，請他重開 Claude Code，
啟用這個 MCP 之後，**第一件事跑 `diagnose()`**。

那支會告訴他 token 有沒有效、實際能呼叫哪些函式。接不上時第一個該跑的就是它。

接著跳到第四步。

#### 情況 B：失敗，出現 `cannotcreatetoken` 或 `servicenotavailable`

**不要停下來，也不要叫他去找系統管理員。** 還有第二條路，而且不需要任何權限。

告訴他：

> 學校把「自己領 token」這個權限關掉了。這很常見，而且從外面探測不出來，
> 只有實際去領才知道。
>
> 但還有一條路：**借用你已經登入的瀏覽器**。
> 這條路不需要 API、不需要權限，只要你自己登得進 Moodle 就一定走得通。

然後請他做兩件事：

1. 跑 `python start_debug_chrome_moodle.py`
2. **在跳出來的那個瀏覽器視窗，用他自己的帳號登入 Moodle**

登入之後你就可以用 `cdp_status()` 確認連得上，
再用 CDP 那幾支工具（`probe_course_access`、`enrolment_details`、
`find_hidden_courses`）讀資料。

原理要跟他講清楚：那些工具是驅動他已經登入的瀏覽器去讀畫面，
**工具本身沒有他的密碼，也不需要**。

### 第四步：讀出他的課，然後做一件有價值的事

不管走 A 還是 B，接下來都一樣。

先列出他看得到的課，**依學年分組**（義守的課號前三碼就是學年，例如 `115118A1806300` 是 115 學年）。

然後問他：**「哪一門課你想仔細看？」**

他挑好之後，把那門課的以下東西整理出來：

1. **每個單元有什麼教材**（`list_materials`）
2. **有幾份作業、各自的截止日**（`list_homework`）
3. **逐份作業的繳交率**（`list_submissions`）

接著做這件事——**這才是重點**：

> 把每份作業的日期抽出來，排成時間軸，看這門課的**實際教學節奏**。
>
> 找出：哪幾週密集、哪幾週空白、繳交率有沒有斷崖。

然後用白話告訴他你看到什麼。例如：

> 「你這門課從 3/02 到 3/23 每週一份作業，然後空了四週（期中考週），
>  4/20 之後又恢復每週一份。W10 那份的繳交率從 30 筆掉到 4 筆——
>  那一週的要求可能跳太高。」

**這叫「遠讀」**：不逐份細看，把整批資料拉遠了看趨勢。
這種分析放進教學實踐計畫、系所評鑑、升等資料，都是高度加分的東西——
不是因為結果一定好看，而是因為**你有資料可以談**。

---

## 🔴 紀律（這幾條請嚴格遵守）

1. **唯讀。** 這套工具刻意不含任何寫入功能。
   不要嘗試改成績、改課程設定，也不要幫他寫這種程式。
   唯讀弄錯了頂多是資料不對；寫入弄錯了是真的改到學生成績。

2. **不要碰密碼。** 帳號密碼一律由他在自己的終端機處理。
   你不問、不存、不寫進任何檔案。

3. **學生個資要小心。** 名單、成績、繳交紀錄都是個資。
   分析可以做，但如果他要把結果寫進文件或簡報，
   **主動提醒他去識別化**——只留人數與分布，不留姓名學號。

4. **資料不足就明講。** 例如課程沒開完成度追蹤，就是沒有點閱資料。
   不要用估計值補，也不要說「應該有人看過」。

5. **看不到的課不代表不存在。** Moodle 只回「你目前還有選課關係」的課。
   選課關係被停用的課會從清單裡消失，而且**完全不報錯**。
   如果他覺得少了幾年的課，用 `probe_course_access(course_id)` 逐一探測，
   不要直接下結論說「那些年的資料沒了」。

---

## 卡住的時候

| 症狀 | 先做這個 |
|---|---|
| 什麼都讀不到 | 跑 `diagnose()` |
| 回「未登入」 | 去 debug Chrome 那個視窗登入 Moodle |
| `accessexception` | 跑 `diagnose()` 看 token 含哪些函式 |
| `invalidtoken` | token 過期了（預設 12 週），重跑 `get_token.py` |
| 少了幾年的課 | `probe_course_access(course_id)` 逐一探測 |

完整的踩坑紀錄在 repo 的 `REVERSE_ENGINEERING.md`，每一條都附實測證據。
