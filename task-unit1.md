# 任務：幫我確認環境真的通了

> 這是義守大學 AIEA 工作坊（2026-08-21）單元 1 的任務檔。
> 把它放進你的工作目錄，然後對 Claude 說：**「讀 任務-單元1.md 照做」**

> ⚠️ **這個檔要等 Claude 桌面版裝好、而且開得起來之後才用得上。**
> 還在安裝階段的話，請照現場投影片或課程網頁上的步驟做完前面六步，
> 再回來用這個檔。

---

## 給 Claude 的指示

你正在協助一位大學老師，確認他的環境真的設定好了。

**這位老師不是工程師。** 檢查完用白話講結果，不要貼一堆技術輸出。
每一項只要講「通了」或「還沒通、要做什麼」。

### 第一步：逐項檢查，然後給他一張表

請實際去查（不要用問的、也不要用猜的），逐項確認：

| 要確認的事 | 怎麼查 |
|---|---|
| **Git 裝好了沒** | 執行 `git --version` |
| **Python 有沒有** | 執行 `python --version`（下午單元 3 會用到；沒有也沒關係，先知道就好） |
| **現在在哪個資料夾** | 印出目前的工作目錄 |
| **這個資料夾裡有什麼** | 列出檔案（如果是空的，就說是空的） |
| **寫得進去嗎** | 建一個 `測試.txt` 寫一行字，確認建得起來，**然後把它刪掉** |

查完給他一張表，每一項標「✅ 通了」或「❌ 還沒」。

### 第二步：❌ 的項目，告訴他怎麼補

**Git 沒有** → 這是今天唯一非裝不可的。請他去
`git-scm.com/download/win`（Mac 用 `git-scm.com/download/mac`），
選 64-bit 版本，一路下一步到底，所有選項都不用改。
**裝完一定要把 Claude 關掉再重開**——Claude 是啟動時才去找 Git 的，不重開會繼續說找不到。

**Python 沒有** → 不急，上午用不到。下午單元 3 才需要，到時候再裝。

**寫不進去** → 可能是選到了唯讀的位置（例如 `C:\Program Files` 底下）。
建議他改挑桌面或 `C:\` 底下自己建的資料夾。

### 第三步：全部 ✅ 之後，講一句話讓他安心

用一句話總結，例如：

> 都通了。你現在這個對話**讀得到也寫得回**你電腦上的檔案——
> 這就是今天下午所有事情的基礎。

**然後停下來。** 不要主動幫他做別的事，等他下一個指示。

---

## 🔴 紀律

1. **不要幫他裝東西。** 安裝程式要按「同意」「下一步」，那些是他的決定，不是你的。
   你只負責檢查與說明。

2. **不要碰帳號密碼。** 登入 Claude、註冊、升級方案都是他自己在介面上做。

3. **建的測試檔要刪掉。** 不要在他的資料夾留垃圾。

4. **查不到就說查不到。** 例如 `git --version` 沒有輸出，就說「查不到，可能沒裝」，
   不要說「應該有裝」。

---

## 順帶一提：Git 是什麼，為什麼非裝不可

如果他問起，這樣解釋：

Git 是一套**檔案的版本記錄器**，你可以想成 Word 的「追蹤修訂」，
但管的是**整個資料夾**：誰在哪一刻改了哪個檔、改成什麼樣，全部留底，隨時退得回去。

**為什麼 Claude 需要它**：你等一下要做的事，是把整個資料夾交給它、
讓它**直接改你的檔案**——不是貼一段字進聊天室而已。
它可能改壞、可能改多。所以在動你的檔案之前，它會先用 Git 把資料夾當下的樣子記下來。
改壞了，一句「還原」就回得去。

**沒有 Git 就沒有這張安全網，所以它乾脆不讓你開始。**

裝了不會怎樣——它安靜待在背景，不跳出來、不佔桌面、不用註冊帳號。
今天全程不會叫你打任何一行 Git 指令，都是 Claude 在背後自己用。
